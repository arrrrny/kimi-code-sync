---
"@moonshot-ai/kimi-code": minor
---

Add the /substitute-model command to configure automatic model fallback when the primary model hits a rate limit. Enable via [experimental] substitute_model = true in config.toml or KIMI_CODE_EXPERIMENTAL_SUBSTITUTE_MODEL=1.
