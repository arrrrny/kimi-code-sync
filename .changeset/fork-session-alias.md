---
"@moonshot-ai/kimi-code": patch
---

`/fork-session` is now an alias for the existing `/fork` slash command, and `/fork-and-switch` is a new slash command that forks the current session and immediately switches into the fork.