---
"@moonshot-ai/kimi-code": minor
---

Add the /update-all-session-models command to switch the working model for every active session at once, reusing the /model picker and asking for confirmation before applying.
