---
"@moonshot-ai/kimi-code": minor
"@moonshot-ai/agent-core-v2": minor
---

Add a `[subscription]` config section to control credit-consuming managed features. Set `web_search = false` or `fetch_url = false` in config.toml to turn web search or URL fetching off; automatic session titles are off by default and turn on with `auto_session_title = true`.
