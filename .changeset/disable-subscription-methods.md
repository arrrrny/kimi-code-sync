---
"@moonshot-ai/kimi-code": minor
"@moonshot-ai/agent-core-v2": minor
---

Add a `[subscription]` config section to turn off credit-consuming managed features. Set `web_search = false`, `fetch_url = false`, or `auto_session_title = false` in config.toml to disable web search, URL fetching, or automatic session titles.
